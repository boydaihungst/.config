# LSP configurations

File name = `lspserver.lua`. Get lsp server name from command `:Mason`.  
For example: `✓ bash-language-server bashls (keywords: ...)` -> lsp server = `bashls`  
Put config here, so you don't have to call vim.lsp.config.
