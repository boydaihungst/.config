#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc
[[ -f ~/.profile ]] && . ~/.profile
# Load git completion
# [[ -f ~/.git-completion.bash ]] && . ~/.git-completion.bash
# Load git aliases
# [[ -f ~/.git-aliases.bash ]] && . ~/.git-aliases.bash
