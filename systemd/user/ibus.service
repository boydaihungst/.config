[Unit]
Description=Ibus daemon


[Service]
Type=simple
; Environment=SSH_AUTH_SOCK=%t/ssh-agent.socket
# DISPLAY required for ssh-askpass to work
Environment=DISPLAY=:0
ExecStart=/usr/bin/ibus-daemon --desktop=i3

[Install]
WantedBy=default.target
